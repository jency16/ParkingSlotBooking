<h1>Booking details</h1>
<div>
  <p>Entry point: <%= @slot_booking.entry_point_id %></p>
  <p>Entry time: <%= @slot_booking.entry_time.strftime('%B %d, %Y %l:%M %p') %></p>
  <p>Registration Number: <%= @slot_booking.vehicle_registration_number %></p>
  <p>Slot: <%= @slot_booking.slot_id %></p>
</div>
<%if !@slot_booking.canceled %>
  <%= button_to "Cancel booking", cancel_slot_booking_path(@slot_booking), method: :patch, data: { confirm: "Are you sure you want to cancel this booking?" }, class: "btn btn-danger" %>
<%else %>
  <p class="badge text-bg-secondary">Canceled !</p>
<%end %>

<br><br>
<br><br>
<%= link_to "Back",slot_bookings_path, class: "btn btn-secondary" %>
